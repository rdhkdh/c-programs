#include<stdio.h>
//library program, creata a better one
struct library
{
    int accno;
    char title[50];
    char author[20];
    int price;
    int flag;
} bk[3]; /*defined an array of objects within the stucture. The object declaration could be done within the main function[local variable] or 
right after structure specification.[global variable] */
int main()
{
    //make the best library program!
}